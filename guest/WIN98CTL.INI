[connection]
; QEMU user networking always exposes the physical host at 10.0.2.2.
; Keep this value when moving a QEMU disk image to another computer.
; For VMware or a raw physical/virtual Windows installation, replace it with
; the IPv4 address of the broker host reachable by that guest.
host=10.0.2.2
port=9898
