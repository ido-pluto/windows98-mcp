[connection]
; IP address of the host on the VM-only/host-only network.
host=192.168.56.1
port=9898

[identity]
guest_id=win98-vm

[security]
; Exactly 64 hexadecimal characters (32 random bytes).
; The same secret must be configured on the host broker.
psk_hex=REPLACE_WITH_64_HEX_CHARACTERS
