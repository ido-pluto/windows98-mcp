/**
 * Guest release accepted by host diagnostics and live smoke tests.
 *
 * Keep this synchronized with BUILD_ID in guest/win98ctl.c when producing a
 * new guest package.
 */
export const EXPECTED_GUEST_BUILD_ID = "win98ctl-0.4.0";
