{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Windows 98 MCP Admin window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
